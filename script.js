// You can add JavaScript functionality here later, for example:
// - Smooth scrolling for navigation links (though CSS scroll-behavior: smooth is already doing this)
// - Dynamic content loading
// - Form submissions for contact
// - More complex animations or interactive elements

// A simple console log to confirm the script is loaded
console.log("Portfolio script loaded!");

// Example: Add a simple hover effect to project cards using JS (optional, as CSS already handles this)
/*
document.addEventListener('DOMContentLoaded', () => {
    const projectCards = document.querySelectorAll('.project-card');

    projectCards.forEach(card => {
        card.addEventListener('mouseenter', () => {
            card.style.transform = 'translateY(-10px) scale(1.03)';
            card.style.boxShadow = '0 20px 40px rgba(0, 0, 0, 0.15)';
        });
        card.addEventListener('mouseleave', () => {
            card.style.transform = 'translateY(0) scale(1)';
            card.style.boxShadow = '0 10px 20px rgba(0, 0, 0, 0.1)'; // Revert to original shadow
        });
    });
});
*/